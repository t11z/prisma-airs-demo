# Environment Configuration Samples

The `env/` directory holds example `.tfvars` files for different lab configurations. Copy and customize these files (e.g., `demo.tfvars`) before running Terraform to match your Azure subscription and resource group.
