name: Deploy Prompt Flow

on:
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: TODO Configure Prompt Flow deployment steps
        run: echo "Add flow packaging and deployment steps here."
